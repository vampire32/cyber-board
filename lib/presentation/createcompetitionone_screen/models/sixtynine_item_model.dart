import '../../../core/app_export.dart';/// This class is used in the [sixtynine_item_widget] screen.
class SixtynineItemModel {SixtynineItemModel({this.limited, this.isSelected, }) { limited = limited  ?? "Limited";isSelected = isSelected  ?? false; }

String? limited;

bool? isSelected;

 }
