import '../../../core/app_export.dart';/// This class is used in the [win_item_widget] screen.
class WinItemModel {WinItemModel({this.win, this.isSelected, }) { win = win  ?? "Win";isSelected = isSelected  ?? false; }

String? win;

bool? isSelected;

 }
